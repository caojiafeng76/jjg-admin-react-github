Scenario: Remove confirmed-unused `formatNumberWithSeparator` from `src/utils/format.ts`.

Reference audit:
- `rg -n --hidden --glob '!node_modules' --glob '!dist' --glob '!bun.lockb' 'formatNumberWithSeparator' .` before edit returned only `src/utils/format.ts:35` (the declaration).
- `git grep -n 'formatNumberWithSeparator' -- . ':!node_modules' ':!dist'` after edit returned no matches (exit 1, no output).

Change verification:
- `git diff --check -- src/utils/format.ts` exited 0.
- Targeted diff: `src/utils/format.ts | 21 deletions(-)`; `formatNumber` remains unchanged.

Typecheck:
- Invocation: `bun run typecheck`
- Observable: `$ tsc -b`
- Exit code: 0
