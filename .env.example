# 环境变量配置模板

# Supabase 配置
# 请将以下值替换为你的实际 Supabase 项目配置
# 可以在 Supabase 项目设置中找到这些值

VITE_REACT_APP_SUPABASE_URL=your_supabase_project_url
VITE_REACT_APP_SUPABASE_KEY=your_supabase_anon_key

# 可选: 仅供 CLI / MCP 使用的远程数据库连接串
# 当 bun run db:push 默认 linked 直连失败时，可设置这个值作为回退
# 不要把真实密码提交到仓库
SUPABASE_DB_URL=
