# 项目更新日志规则

- 根目录 `changelog.md` 是项目统一更新日志。
- 以后每次修改代码、配置、脚本、SQL、指令或文档，都必须在同一工作区变更中于 `changelog.md` 顶部追加记录；仅补充 changelog 时无需重复记录。
- 每条记录包含日期、类别、范围、摘要和验证结果；最新记录置顶，不改写或删除历史记录。
- 这是项目级强制规则，AI 助手处理任务时必须执行。